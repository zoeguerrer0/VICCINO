/*module.exports = {
    env: {
        browser: true, // Si también estás trabajando en el navegador
        es2021: true,
        node: true, // Permite el uso de `module` y otras variables de Node.js
    },
    extends: [
        'eslint:recommended', // Extiende las reglas recomendadas de ESLint
        // otras configuraciones si es necesario
    ],
    parserOptions: {
        ecmaVersion: 12, // Usa la versión de ECMAScript que prefieras
        sourceType: 'module', // Permite el uso de módulos ES
    },
    rules: {
        // Aquí puedes añadir tus reglas personalizadas
    },
};*/
